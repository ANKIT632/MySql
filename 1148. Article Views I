# Write your MySQL query statement below
# Write your MySQL query statement below
# order by sort the data
# distinct does not give same ouput.
select distinct author_id as id from Views where author_id = viewer_id order by id;
